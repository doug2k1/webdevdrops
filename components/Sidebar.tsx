interface Props {
  area?: string
}

export function Sidebar({ area }: Props) {
  return null

  // const { locale } = useRouter()

  // if (locale && locale !== 'pt-BR') {
  //   return null
  // }
}
